VSFTPD

-----------DMZ2----------------
sudo useradd -m -s /bin/bash patrick
echo "patrick:P@ssw0rd" | sudo chpasswd
/etc/vsftpd.conf
listen=NO
pasv_enable=NO # Désactiver le mode passif

# Activer FTPS
ssl_enable=YES
ssl_enable=YES
rsa_cert_file=/home/worldskills/ftp.crt
rsa_private_key_file=/home/worldskills/ftp.key

allow_writeable_chroot=YES
chroot_local_user=YES

-----------DMZ3----------------
apt install proftpd
useradd -m -s /bin/bash thierry
echo "thierry:P@ssw0rd" |  chpasswd
/etc/proftpd/proftpd.conf

PassivePorts 49152 65534
DefaultRoot ~
<IfModule mod_tls.c>
    TLSEngine                   on
    TLSLog                      /var/log/proftpd/tls.log
    TLSProtocol                 SSLv23
    TLSRSACertificateFile       /etc/ssl/certs/proftpd.pem
    TLSRSACertificateKeyFile    /etc/ssl/private/proftpd.pem
    TLSOptions                  NoCertRequest
    TLSVerifyClient             off
    TLSRequired                 on
</IfModule>


####
verifier le service -> proftpd -t