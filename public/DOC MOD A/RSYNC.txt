RSYNC

apt install rsync (sur les deux serveurs)
ssh-keygen
3x enter
ssh-copy-id root@srvpv02.fast-provider.net

/usr/local/bin/backup.sh
#!/bin/bash
rsync -avz  /data/* root@10.1.20.1:/backup

chmod +x /usr/local/bin/backup.sh
crontab -e
 */10 * * * * /usr/local/bin/backup.sh